<UserControl x:Class="MonitorApp.Views.Pages.TrafficMonitor.FooterParts.TM_Footer_UploadColumn"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="40"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <iconPacks:PackIconFeatherIcons Kind="ArrowUp" Width="18" Height="18" Foreground="#F9A8D4"
                                        Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center"/>

        <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="12,0,0,0">
            <TextBlock Text="{Binding UploadTotal}" FontSize="18" FontWeight="SemiBold" Foreground="#2B3440"/>
            <TextBlock Text="{Binding UploadSpeed}" FontSize="12" Foreground="#7C8591" Margin="0,4,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>