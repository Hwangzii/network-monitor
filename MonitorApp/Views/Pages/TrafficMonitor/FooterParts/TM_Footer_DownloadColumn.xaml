<UserControl x:Class="MonitorApp.Views.Pages.TrafficMonitor.FooterParts.TM_Footer_DownloadColumn"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="40"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,12,0">
            <TextBlock Text="{Binding DownloadTotal}" FontSize="18" FontWeight="SemiBold" Foreground="#2B3440"/>
            <TextBlock Text="{Binding DownloadSpeed}" FontSize="12" Foreground="#7C8591" Margin="0,4,0,0"/>
        </StackPanel>

        <iconPacks:PackIconFeatherIcons Kind="ArrowDown" Width="18" Height="18" Foreground="#FBBF24"
                                        Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center"/>
    </Grid>
</UserControl>